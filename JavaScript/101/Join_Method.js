let values = ["Welcome", "To", "JavaScript"];

let Join = values.join("$");

console.log(Join);

let sizeType = ["Small", "Medium", "Large"]; // array
sizeType[sizeType.length - 1] = "Extra Large"; // L -> Extra Large
console.log(sizeType.length); // 3
